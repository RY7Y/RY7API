# wrangler.toml
# 📌 إعداد Cloudflare Worker لمشروع RY7 API
# يتكامل مع D1 Database لتخزين الأكواد واستخدامها بشكل دائم

# 🏷️ اسم الـ Worker
name = "ry7api"

# 📂 ملف التشغيل الأساسي
main = "worker.js"

# 📅 تاريخ التوافق (استخدم تاريخ اليوم لضمان دعم API الحديث)
compatibility_date = "2025-09-24"

# 🌍 ربط الـ Worker مع دومينك عبر Cloudflare
routes = [
    { pattern = "devry7yy.org/api/*", zone_name = "devry7yy.org" }
]

# 🔧 متغيرات البيئة (Environment Variables)
[vars]
# بإمكانك تخليه فاضي إذا بتعتمد فقط على D1
GITHUB_CODES_URL = "https://devry7yy.org/codes.json"

# 🗄️ تعريف قاعدة بيانات D1
[[d1_databases]]
binding = "RY7_CODES"         # نفس الاسم اللي استخدمناه داخل worker.js
database_name = "ry7-codes"   # اسم القاعدة
database_id = "adcd2714-2ed4-4e6d-a326-880b81bf44d4"  # يطلع لك من Cloudflare لما تنشئ القاعدة